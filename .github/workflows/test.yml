name: Tests CI

on:
  push:
    branches: [ junior ]
  pull_request:
    branches: [ junior ]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]

    steps:
    - uses: actions/checkout@v3

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Create dummy Firebase credentials
      run: |
        echo '{"type":"service_account","project_id":"dummy","private_key":"dummy_key"}' > server/taskService/serviceAccountKey.json

    - name: Run type checking
      run: npm run check
      env:
        FIREBASE_SERVICE_ACCOUNT: '{"type":"service_account","project_id":"dummy","private_key":"dummy_key"}'

    - name: Run unit and integration tests
      run: npm test
      env:
        FIREBASE_SERVICE_ACCOUNT: '{"type":"service_account","project_id":"dummy","private_key":"dummy_key"}'

    - name: Run E2E tests
      run: npm run test:e2e
      env:
        FIREBASE_SERVICE_ACCOUNT: '{"type":"service_account","project_id":"dummy","private_key":"dummy_key"}'

    - name: Upload test results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: test-results
        path: test-results/